namespace Doublsb.Dialog
{
    // TODO: remove this enum
    public enum CommandId
    {
        print,
        color,
        e,//emote
        size,
        sound,
        speed,
        click,
        close,
        wait
    }
}